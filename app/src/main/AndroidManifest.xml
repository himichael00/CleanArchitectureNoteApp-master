<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.plcoding.cleanarchitecturenoteapp">

    <application
        android:name=".NoteApp"
        android:allowBackup="true"
        android:icon="@drawable/stickynote"
        android:label="Sok Sibuk Note"
        android:roundIcon="@drawable/stickynote"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanArchitectureNoteApp">
        <activity
            android:name=".features.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CleanArchitectureNoteApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>